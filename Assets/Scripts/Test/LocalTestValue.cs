using UnityEngine;

public sealed class LocalTestValue
{
    public static readonly int tilesPerChunk = 10;
    public static readonly Vector2 focusAtPlayer = new Vector2(0, 0);
    public static readonly int maxChunkWorkAroundPlayer = 3;
}
